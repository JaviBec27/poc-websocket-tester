import { Routes } from '@angular/router';
import { WebsocketTestComponent } from './pages/websocket-test/websocket-test.component';

export const routes: Routes = [
  { path: '', component: WebsocketTestComponent }
];
